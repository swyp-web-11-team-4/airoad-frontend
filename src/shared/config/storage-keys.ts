export const COOKIE_KEYS = {
  ACCESS_TOKEN: "access_token",
  REFRESH_TOKEN: "refresh_token",
} as const;

export const SESSION_STORAGE_KEYS = {
  AUTH_REDIRECT_TARGET: "auth_redirect_target",
} as const;
