import { style } from "@vanilla-extract/css";

export const menu = style({
  width: 150,
});

export const trigger = style({
  cursor: "pointer",
});

export const deleteAccount = style({
  color: "var(--red-11)",
});
